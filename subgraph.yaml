specVersion: 0.0.6
description: curve-sps substreams based subgraph
repository: # fill in with git remote url
schema:
  file: ./schema.graphql

dataSources:
  - kind: substreams
    name: curve-sps
    network: mainnet
    source:
      package:
        moduleName: graph_out
        file: curve-sps-v0.1.21-alpha.spkg
    mapping:
      kind: substreams/graph-entities
      apiVersion: 0.0.5
